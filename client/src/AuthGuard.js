//import store from "./store";

export default (to, from, next) => {
  /* Fix later. store.getters.user needs to be Apollo Graph QL Cash for User
  if (!store.getters.user) {
    next({
      path: "/signin"
    });
  } else {
    next();
  }
  */
};